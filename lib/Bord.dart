

import 'package:flutter/material.dart';

class Bord extends StatelessWidget {
  const Bord({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(height: 30,width: 30,color: Colors.white);
  }
}
